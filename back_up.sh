look at files in directory: Get file last change time
    get file last change time on git hub
        if file is not on git hub get name
            git add file_name
            git commit -m " File_name added on DATE TIME"
        else commit -m "File_name updated DATE TIME"
        
